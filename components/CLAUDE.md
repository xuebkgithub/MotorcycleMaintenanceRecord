# Components 模块 - 自定义组件文档

> **面包屑导航**：[根目录](../CLAUDE.md) > **Components 模块**
>
> 生成时间：2025-12-01 18:00:48
> 模块类型：微信小程序自定义组件

---

## 📋 模块概述

`components/` 模块包含本项目的**自定义组件集合**，主要用于辅助 Demo 页面展示和增强用户体验。这些组件不是 TDesign 组件库的一部分，而是项目特有的功能组件。

### 关键特性

- ✅ 4 个自定义组件
- ✅ 统一的 Demo 展示结构
- ✅ 支持多插槽（multipleSlots）
- ✅ 组件化封装，复用性强
- ✅ 全局注册，所有页面可用

---

## 🗂️ 组件列表

### 1. **demo-header（Demo 页面头部）**

#### **功能描述**
用于 Demo 页面顶部的标题和描述展示，提供统一的页面头部样式。

#### **文件结构**
```
components/demo-header/
├── index.js        # 组件逻辑
├── index.json      # 组件配置
├── index.wxml      # 组件模板
├── index.wxss      # 组件样式
└── index.d.ts      # TypeScript 类型定义
```

#### **属性（Properties）**
| 属性名 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| title | String | - | 否 | 页面标题 |
| desc | String | - | 否 | 页面描述 |
| notice | String | - | 否 | 注意事项（可选） |

#### **插槽（Slots）**
- 支持多插槽（`multipleSlots: true`）

#### **使用示例**
```wxml
<t-demo-header
  title="按钮"
  desc="按钮用于开启一个闭环的操作任务，如提交表单。"
  notice="注意：按钮需要根据场景选择合适的类型"
/>
```

#### **应用场景**
- Demo 页面顶部标题展示
- 组件库文档页面头部
- 统一的页面入口样式

---

### 2. **demo-block（Demo 展示块）**

#### **功能描述**
用于包裹 Demo 示例代码的容器组件，提供标题、描述和操作按钮等功能，是 Demo 页面的核心展示组件。

#### **文件结构**
```
components/demo-block/
├── index.js        # 组件逻辑
├── index.json      # 组件配置
├── index.wxml      # 组件模板
├── index.wxss      # 组件样式
└── index.d.ts      # TypeScript 类型定义
```

#### **属性（Properties）**
| 属性名 | 类型 | 默认值 | 必填 | 说明 |
|--------|------|--------|------|------|
| title | String | '' | 否 | 示例标题 |
| desc | String | '' | 否 | 示例描述 |
| operList | Array | - | 否 | 操作按钮列表 |
| padding | Boolean | false | 否 | 是否添加内边距 |

#### **事件（Events）**
| 事件名 | 说明 | 回调参数 |
|--------|------|----------|
| clickoper | 点击操作按钮时触发 | type（按钮类型） |

#### **插槽（Slots）**
- 默认插槽：用于放置 Demo 示例内容
- 支持多插槽（`multipleSlots: true`）

#### **使用示例**
```wxml
<t-demo title="基础用法" desc="最简单的按钮用法">
  <t-button>按钮</t-button>
</t-demo>

<t-demo
  title="带操作的示例"
  desc="支持操作按钮"
  operList="{{operList}}"
  bind:clickoper="handleOper"
>
  <view>示例内容</view>
</t-demo>
```

#### **应用场景**
- 包裹组件示例代码
- 提供示例标题和说明
- 支持示例代码操作按钮（如查看代码、复制等）

---

### 3. **pull-down-list（下拉列表）**

#### **功能描述**
实现下拉列表选择功能的自定义组件，用于数据选择和筛选场景。

#### **文件结构**
```
components/pull-down-list/
├── index.js        # 组件逻辑
├── index.json      # 组件配置
├── index.wxml      # 组件模板
├── index.wxss      # 组件样式
└── index.d.ts      # TypeScript 类型定义
```

#### **属性（Properties）**
（需根据实际代码补充）

#### **事件（Events）**
（需根据实际代码补充）

#### **使用示例**
```wxml
<pull-down-list
  list="{{list}}"
  bind:select="handleSelect"
/>
```

#### **应用场景**
- 数据筛选
- 选项选择
- 下拉菜单

---

### 4. **trd-privacy（隐私协议）**

#### **功能描述**
用于展示和处理小程序隐私协议的组件，符合微信小程序隐私保护指引要求。

#### **文件结构**
```
components/trd-privacy/
├── privacy.js      # 组件逻辑
├── privacy.json    # 组件配置
├── privacy.wxml    # 组件模板
└── privacy.wxss    # 组件样式
```

#### **核心方法**
- `showPrivacyWin()` - 显示隐私协议弹窗

#### **使用示例**
```wxml
<!-- 在 home 页面中使用 -->
<trd-privacy id="trdPrivacy" />
```

```javascript
// 在页面逻辑中调用
Page({
  onLoad() {
    this.trdPrivacy = this.selectComponent('#trdPrivacy');
  },

  showPrivacyWin() {
    this.trdPrivacy.showPrivacyWin();
  },
});
```

#### **应用场景**
- 小程序首次启动时展示隐私协议
- 用户主动查看隐私政策
- 符合微信小程序合规要求

---

## 🔌 接口与依赖

### 组件注册方式

#### **全局注册（app.json）**
```json
{
  "usingComponents": {
    "t-demo": "./components/demo-block/index",
    "t-demo-header": "./components/demo-header/index"
  }
}
```

#### **页面级注册（page.json）**
```json
{
  "usingComponents": {
    "pull-down-list": "/components/pull-down-list/index",
    "trd-privacy": "/components/trd-privacy/privacy"
  }
}
```

### 依赖关系

#### **内部依赖**
- 无内部依赖（组件独立）

#### **外部依赖**
- 微信小程序组件基础能力
- 部分组件可能依赖 TDesign 样式变量

### 被依赖关系

所有 `pages/` 目录下的 Demo 页面都依赖 `demo-header` 和 `demo-block` 组件。

---

## 🎯 组件设计模式

### 通用组件结构

#### **组件定义**
```javascript
Component({
  options: {
    multipleSlots: true, // 启用多插槽
  },

  properties: {
    // 组件属性定义
  },

  data: {
    // 组件内部数据
  },

  methods: {
    // 组件方法
  },

  lifetimes: {
    // 生命周期函数
  },
});
```

#### **组件配置（component.json）**
```json
{
  "component": true,
  "usingComponents": {
    // 引用其他组件
  }
}
```

### 事件通信模式

#### **子组件向父组件传递事件**
```javascript
// 子组件（demo-block）
methods: {
  clickHandle(e) {
    const { type } = e.currentTarget.dataset;
    this.triggerEvent('clickoper', type); // 触发自定义事件
  },
}

// 父组件（页面）
<t-demo bind:clickoper="handleOper">...</t-demo>

handleOper(e) {
  const type = e.detail; // 接收事件参数
  // 处理逻辑
}
```

---

## 📐 组件开发规范

### 1. **命名规范**
- **组件目录名**：kebab-case（如 `demo-header`）
- **组件文件名**：
  - 通用组件：`index.js`, `index.wxml`, `index.wxss`
  - 特殊组件：`[component-name].js`（如 `privacy.js`）
- **组件在页面中使用**：
  - 全局注册：`t-` 前缀（如 `<t-demo>`）
  - 局部注册：原名或自定义名（如 `<trd-privacy>`）

### 2. **属性定义规范**
```javascript
properties: {
  title: {
    type: String,      // 属性类型
    value: '',         // 默认值
    required: false,   // 是否必填（可选）
  },
}
```

### 3. **多插槽支持**
```javascript
Component({
  options: {
    multipleSlots: true, // 必须启用
  },
});
```

```wxml
<!-- 组件模板 -->
<view class="header">
  <slot name="header"></slot>
</view>
<view class="content">
  <slot></slot> <!-- 默认插槽 -->
</view>
<view class="footer">
  <slot name="footer"></slot>
</view>
```

### 4. **样式隔离**
```javascript
Component({
  options: {
    styleIsolation: 'isolated', // 样式隔离模式
  },
});
```

---

## 🛠️ 常见操作

### 添加新组件

#### **步骤 1：创建组件目录**
```bash
mkdir components/new-component
```

#### **步骤 2：创建组件文件**
```bash
cd components/new-component
touch index.js index.json index.wxml index.wxss index.d.ts
```

#### **步骤 3：编写组件代码**
```javascript
// index.js
Component({
  properties: {
    // 定义属性
  },
  methods: {
    // 定义方法
  },
});
```

```json
// index.json
{
  "component": true,
  "usingComponents": {}
}
```

#### **步骤 4：全局注册（可选）**
在 `app.json` 中添加：
```json
{
  "usingComponents": {
    "new-component": "./components/new-component/index"
  }
}
```

#### **步骤 5：在页面中使用**
```wxml
<new-component prop="value" />
```

### 修改现有组件

1. **找到组件文件**：`components/[component-name]/`
2. **修改组件逻辑**：编辑 `index.js`
3. **修改组件样式**：编辑 `index.wxss`
4. **修改组件模板**：编辑 `index.wxml`
5. **测试验证**：在使用该组件的页面中测试

### 调试组件

1. **开发者工具**：使用微信开发者工具的调试面板
2. **console.log**：在组件方法中打印日志
3. **断点调试**：在开发者工具中设置断点
4. **样式调试**：使用 wxml 面板查看样式应用情况

---

## 🧪 测试与验证

### 组件测试清单

- ✅ **属性传递**：所有属性能正确传递和使用
- ✅ **默认值**：属性默认值生效
- ✅ **事件触发**：自定义事件能正确触发和传递参数
- ✅ **插槽内容**：插槽内容能正确渲染
- ✅ **样式隔离**：组件样式不影响外部，外部样式不影响组件
- ✅ **响应式**：组件能响应数据变化（setData）
- ✅ **生命周期**：组件生命周期钩子正常执行
- ✅ **跨页面复用**：在不同页面中使用表现一致

### 测试方法

#### **单元测试（手动）**
1. 创建测试页面
2. 引用组件
3. 测试各种属性组合
4. 验证事件回调
5. 检查样式表现

#### **集成测试**
1. 在实际 Demo 页面中使用
2. 验证与其他组件的协同工作
3. 测试在不同渲染模式下的表现（Webview / Skyline）

---

## 📊 组件统计

- **总组件数**：4
- **全局注册组件**：2（demo-header, demo-block）
- **页面级注册组件**：2（pull-down-list, trd-privacy）
- **支持多插槽的组件**：2（demo-header, demo-block）
- **带事件通信的组件**：1（demo-block）

---

## 🔗 相关链接

- [返回根目录文档](../CLAUDE.md)
- [Pages 模块文档](../pages/CLAUDE.md)
- [微信小程序自定义组件文档](https://developers.weixin.qq.com/miniprogram/dev/framework/custom-component/)

---

## 💡 最佳实践

### 1. **组件复用性设计**
- 属性配置化，避免硬编码
- 提供合理的默认值
- 支持插槽自定义内容
- 暴露必要的事件接口

### 2. **组件性能优化**
- 避免频繁 setData
- 减少不必要的数据监听（observers）
- 使用 `data-*` 传递简单数据
- 合理使用组件生命周期

### 3. **组件可维护性**
- 清晰的属性和事件文档
- 合理的代码注释
- 遵循统一的命名规范
- TypeScript 类型定义（.d.ts）

### 4. **组件安全性**
- 验证外部传入的属性
- 防止 XSS 攻击（特别是动态内容）
- 合理的权限控制（如隐私协议组件）

---

> **文档维护**：本文档由 Claude Code AI 自动生成，基于 `components/` 模块代码结构分析。
> **更新建议**：当组件结构发生变化时，重新运行 `/zcf:init-project` 命令更新文档。
